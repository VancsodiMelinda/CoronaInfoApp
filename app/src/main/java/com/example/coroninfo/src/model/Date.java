package com.example.coroninfo.src.model;

import com.google.gson.annotations.SerializedName;

import java.text.SimpleDateFormat;
import java.util.Calendar;

public class Date {
    /*
    private static final String[] week = generateDates();
    private static final String a = week[0];

    @SerializedName(a)
    private String date0 = null;


    private static String[] generateDates()
    {
        String[] generatedWeek = new String[7];
        SimpleDateFormat formatter = new SimpleDateFormat("yyyy-MM-dd");
        Calendar calendar = Calendar.getInstance();

        for (int i = 0; i < 7; i++)
        {
            calendar.add(Calendar.DATE, -i);
            java.util.Date dt = calendar.getTime();
            generatedWeek[i] = formatter.format((dt));
        }

        return generatedWeek;
    }

     */
}
